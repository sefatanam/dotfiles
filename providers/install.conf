# omp reads its model/provider list from a fixed path under ~/.omp/agent, not
# from ~/.config, so the stowed shell package's ~/.config stub can't reach it.
LINK models.yml -> ~/.omp/agent/models.yml
