# bat config — also the source of delta's syntax highlighting themes.
# Themes in ./themes are compiled into bat's cache by `bat cache --build`
# (setup.sh runs it); delta then finds them via `--syntax-theme=rose-pine`.

--theme="rose-pine"
--style="numbers,changes,header"
