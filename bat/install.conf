# delta reads its syntax themes out of bat's compiled cache, so the Rosé
# Pine .tmTheme files in bat/themes only take effect once the cache rebuilds.
POST if command -v bat >/dev/null 2>&1; then bat cache --build >/dev/null; else warn "bat not installed (skipping syntax theme cache)"; fi
