# On macOS lazygit reads ~/Library/Application Support/lazygit, not
# ~/.config, so the stowed ~/.config/lazygit link isn't enough on its own.
PLATFORM darwin
LINK config.yml -> ~/Library/Application Support/lazygit/config.yml
